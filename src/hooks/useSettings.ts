'use client';

export { useSettings, useTheme, DEFAULT_SETTINGS, ACCENT_PRESETS } from '@/context/SettingsContext';
export type { Settings, Theme, AccentColor, Language, ButtonStyle, FontSize } from '@/context/SettingsContext';
